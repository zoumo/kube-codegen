version: 1
go:
  test:
    exclude:
      - third_party
  mod:
    require:
      - path: sigs.k8s.io/controller-tools
        version: v0.5.0
  format:
    exclude:
      files:
       - "datafile.go"
       - "bindata.go"
